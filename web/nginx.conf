server {
  listen 8080;
  root /usr/share/nginx/html;
  index index.html;
  # SPA fallback: any unknown path serves index.html (client-side routing incl. /callback).
  location / {
    try_files $uri /index.html;
  }
}
